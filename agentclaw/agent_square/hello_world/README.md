# 01 Hello World

最基础的 LLM 对话示例：单轮问答、流式输出与 thread_id 多轮对话。

## 使用方式

在 Dashboard 的模板库中导入该模板后，会复制到当前项目的 `agents/hello_world/` 目录，并注册 workflow `hello_world`。

推荐输入：

```text
你好，简单介绍一下你能做什么。
```

## 包含内容

- `agents/hello_world.py`：workflow 入口文件
