# 05 Parallel Analysis

Fan-out/Fan-in 并行执行示例：同时分析情感、关键词、摘要并合并报告。

## 使用方式

在 Dashboard 的模板库中导入该模板后，会复制到当前项目的 `agents/parallel/` 目录，并注册 workflow `parallel`。

推荐输入：

```text
AgentClaw 让构建生产可用的智能体更简单，支持状态管理、流式输出和工具集成。
```

## 包含内容

- `agents/parallel.py`：workflow 入口文件
